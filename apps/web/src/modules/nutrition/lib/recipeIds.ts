/**
 * Легасі-реекспорт recipe-ids з `@sergeant/nutrition-domain`. Файл
 * залишено, щоб існуючі імпорти `../lib/recipeIds.js` не довелось
 * переписувати всі одразу.
 *
 * Для нового коду: `import { stableRecipeId } from "@sergeant/nutrition-domain";`
 */
export { stableRecipeId } from "@sergeant/nutrition-domain";
export type { StableRecipeIdInput } from "@sergeant/nutrition-domain";
