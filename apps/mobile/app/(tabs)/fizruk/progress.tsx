/**
 * `/fizruk/progress` — Charts + photo progress + backup.
 * Thin wrapper around `@/modules/fizruk/pages/Progress`.
 */

import { Progress } from "@/modules/fizruk/pages/Progress";

export default function FizrukProgressRoute() {
  return <Progress />;
}
